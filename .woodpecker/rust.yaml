when:
  - event: [push, pull_request, manual]
    branch: master

steps:
  - name: build-and-test
    image: rust:latest
    environment:
      CARGO_TERM_COLOR: always
    commands:
      - cargo build --verbose
      - cargo test --verbose
